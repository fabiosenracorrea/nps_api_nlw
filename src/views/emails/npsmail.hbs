<style>
  .container {
    display: flex;
    justify-content: center;
    flex-direction: column;
    align-items: center;
    width: 800px;
    margin: 30px auto 0;
  }

  .value {
    padding: 10px;
    background: #8257e5;
    color: #fff;
    border-radius: 6px;
    width: 15px;
    text-decoration: none;
    text-align: center;
    margin-left: 4px;
  }

  .value:first-child {
    margin-left: 0;
  }

  .level {
    display: flex;
    margin: 10px;
    justify-content: space-between;
    width: 350px;
  }

  .answers {
    width: 350px;
    display: flex;
    justify-content: space-between;
  }
</style>

<div class="container">
  <label>OLá <strong>{{name}}</strong>! Tudo bem?</label>

  <h3>{{title}}</h3>
  <br/>

  <strong>{{description}}</strong>

  <div class="level">
    <span>Pouco Provável</span>
    <span>Muito Provável</span>
  </div>

  <div class="answers">
    <a href="{{baseLink}}/1?id={{users_surveys_id}}" class="value">1</a>
    <a href="{{baseLink}}/2?id={{users_surveys_id}}" class="value">2</a>
    <a href="{{baseLink}}/3?id={{users_surveys_id}}" class="value">3</a>
    <a href="{{baseLink}}/4?id={{users_surveys_id}}" class="value">4</a>
    <a href="{{baseLink}}/5?id={{users_surveys_id}}" class="value">5</a>
    <a href="{{baseLink}}/6?id={{users_surveys_id}}" class="value">6</a>
    <a href="{{baseLink}}/7?id={{users_surveys_id}}" class="value">7</a>
    <a href="{{baseLink}}/8?id={{users_surveys_id}}" class="value">8</a>
    <a href="{{baseLink}}/9?id={{users_surveys_id}}" class="value">9</a>
    <a href="{{baseLink}}/10?id={{users_surveys_id}}" class="value">10</a>
  </div>

  <br/>
  <br/>

  <strong>Sua opinião é muito importante para nós</strong>
  <h3>Equipe | <strong>Fábio SCX</strong></h3>
</div>
